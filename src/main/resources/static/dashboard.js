let selectedOrderNo='';
async function ensurePatient(){const res=await fetch('/api/auth/me');const json=await res.json();if(!json.success||json.data.roleType!==1){location.href='/login';throw new Error('未登录');}}
function statusText(s){return ['待支付','已支付','已取消','已改签'][s]||'未知';}
function switchTab(){loadOrders();}
async function loadOrders(){await ensurePatient();const res=await fetch('/api/order/my');const json=await res.json();const rows=document.getElementById('orderRows');if(!json.success){rows.innerHTML=`<tr><td colspan="5">${json.message}</td></tr>`;return;}if(!json.data.length){rows.innerHTML='<tr><td colspan="5"><div class="empty">暂无预约记录，去科室页选择医生预约。</div></td></tr>';return;}rows.innerHTML=json.data.map(o=>{const time=`${o.schedule_date||o.scheduleDate} ${o.start_time||o.startTime}-${o.end_time||o.endTime}`;return `<tr><td>${o.order_no||o.orderNo}</td><td>${o.department_name||o.departmentName} / ${o.doctor_name||o.doctorName}</td><td>${time}</td><td>${statusText(o.status)}</td><td class="table-actions">${actions(o.order_no||o.orderNo,o.status)}</td></tr>`}).join('');}
function actions(no,status){let html='';if(status===0)html+=`<button class="btn success" onclick="payOrder('${no}')">支付</button>`;if(status===0||status===1)html+=`<button class="btn danger" onclick="cancelOrder('${no}')">取消</button>`;if(status===1)html+=`<button class="btn warning" onclick="showReschedule('${no}')">改签</button>`;return html||'<span class="muted">无操作</span>';}
async function payOrder(no){await ensurePatient();const res=await fetch(`/api/order/no/${no}/pay`,{method:'POST'});const json=await res.json();Swal.fire(json.success?'支付成功':'支付失败',json.message,json.success?'success':'error').then(loadOrders);}
async function cancelOrder(no){await ensurePatient();const ok=await Swal.fire({title:'确认取消预约？',text:'取消后号源将回滚',icon:'warning',showCancelButton:true});if(!ok.isConfirmed)return;const res=await fetch(`/api/order/no/${no}/cancel`,{method:'POST'});const json=await res.json();Swal.fire(json.success?'取消成功':'取消失败',json.message,json.success?'success':'error').then(loadOrders);}
function showReschedule(no){selectedOrderNo=no;document.getElementById('rescheduleModal').style.display='flex';}
function hideReschedule(){document.getElementById('rescheduleModal').style.display='none';}
async function submitReschedule(){await ensurePatient();const newSlotId=Number(document.getElementById('newSlotId').value);const res=await fetch('/api/order/reschedule',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({oldOrderNo:selectedOrderNo,newSlotId})});const json=await res.json();Swal.fire(json.success?'改签成功':'改签失败',json.success?`新订单号：${json.data.newOrderNo}`:json.message,json.success?'success':'error').then(()=>{hideReschedule();loadOrders();});}
loadOrders();
