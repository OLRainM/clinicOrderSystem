function fmt(d){return d.toISOString().substring(0,10);}function range(days){const end=new Date();const start=new Date();start.setDate(end.getDate()-days+1);return{start:fmt(start),end:fmt(end)};}let allDoctors=[],departments=[];
const halfHourSlots=['08:00-08:30','08:30-09:00','09:00-09:30','09:30-10:00','10:00-10:30','10:30-11:00','11:00-11:30','14:00-14:30','14:30-15:00','15:00-15:30','15:30-16:00','16:00-16:30','16:30-17:00'];
async function api(url,opt={}){const res=await fetch(url,opt);if(res.status===401||res.redirected){location.href='/admin/login';throw new Error('未登录');}const json=await res.json();if(!json.success)throw new Error(json.message);return json.data;}
function cnPeriod(p){return p==='AM'?'上午':'下午';}function showPanel(name){document.querySelectorAll('.admin-panel').forEach(p=>p.classList.remove('active'));document.getElementById('panel-'+name).classList.add('active');document.querySelectorAll('.side-link').forEach(a=>a.classList.remove('active'));const m={dashboard:'/admin/dashboard',schedule:'/admin/schedule/manage',doctor:'/admin/doctor/manage',finance:'/admin/finance'};document.querySelectorAll('.side-link').forEach(a=>{if(a.getAttribute('href')===m[name])a.classList.add('active')});}
async function initAdmin(){departments=await api('/admin/api/manage/departments');allDoctors=await api('/admin/api/manage/doctors');const p=location.pathname.includes('schedule')?'schedule':location.pathname.includes('doctor')?'doctor':location.pathname.includes('finance')?'finance':'dashboard';showPanel(p);if(p==='dashboard')loadStats(7);if(p==='doctor')loadDoctors();if(p==='schedule')loadSchedules();}
async function aggregateToday(){try{await api(`/admin/api/stats/aggregate?date=${fmt(new Date())}`,{method:'POST'});Swal.fire('聚合完成','','success');loadStats(7);}catch(e){Swal.fire('聚合失败',e.message,'error');}}
async function loadStats(days){const r=range(days);renderDept(await api(`/admin/api/stats/department?start=${r.start}&end=${r.end}`));renderDoctor(await api(`/admin/api/stats/doctor?start=${r.start}&end=${r.end}`));}
function renderDept(data){echarts.init(document.getElementById('deptChart')).setOption({tooltip:{},xAxis:{type:'category',data:(data||[]).map(x=>x.department_name||x.departmentName||'未知科室')},yAxis:{type:'value'},series:[{name:'就诊量',type:'bar',data:(data||[]).map(x=>x.visit_count||x.visitCount||0)}]});}
function renderDoctor(data){echarts.init(document.getElementById('doctorChart')).setOption({tooltip:{},xAxis:{type:'category',data:(data||[]).map(x=>x.doctor_name||x.doctorName||'医生')},yAxis:{type:'value'},series:[{name:'接诊率',type:'line',smooth:true,data:(data||[]).map(x=>x.reception_rate||x.receptionRate||0)}]});}
function deptOptions(v){return departments.map(d=>`<option value="${d.id}" ${String(v)===String(d.id)?'selected':''}>${d.name}</option>`).join('');}
function doctorOptions(deptId,v){return allDoctors.filter(d=>!deptId||String(d.department_id||d.departmentId)===String(deptId)).map(d=>`<option value="${d.id}" ${String(v)===String(d.id)?'selected':''}>${d.name}（${d.title||'无职称'}）</option>`).join('');}
async function loadDoctors(){allDoctors=await api('/admin/api/manage/doctors');doctorRows.innerHTML=allDoctors.map(d=>`<tr><td>${d.id}</td><td>${d.name}</td><td>${d.title||'未填写'}</td><td>${d.department_name||d.departmentName}</td><td class="table-actions"><button class="btn secondary" onclick="openDoctorForm(${d.id})">编辑</button><button class="btn danger" onclick="deleteDoctor(${d.id})">删除</button></td></tr>`).join('')||'<tr><td colspan="5">暂无医生</td></tr>';}
async function openDoctorForm(id){const d=allDoctors.find(x=>x.id===id)||{};const {value:f}=await Swal.fire({title:id?'编辑医生':'添加医生',html:`<input id="fName" class="swal2-input" placeholder="医生姓名" value="${d.name||''}"><input id="fTitle" class="swal2-input" placeholder="职称" value="${d.title||''}"><select id="fDept" class="swal2-select">${deptOptions(d.department_id||d.departmentId)}</select>`,focusConfirm:false,showCancelButton:true,confirmButtonText:'提交保存',cancelButtonText:'取消',preConfirm:()=>({name:fName.value,title:fTitle.value,departmentId:fDept.value})});if(!f)return;await api(id?`/admin/api/manage/doctors/${id}`:'/admin/api/manage/doctors',{method:id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});Swal.fire('保存成功','','success');loadDoctors();}
async function deleteDoctor(id){if(!(await Swal.fire({title:'确认删除医生？',text:'如果医生已有排班，请先删除排班。',icon:'warning',showCancelButton:true,confirmButtonText:'删除',cancelButtonText:'取消'})).isConfirmed)return;try{await api(`/admin/api/manage/doctors/${id}`,{method:'DELETE'});loadDoctors();}catch(e){Swal.fire('删除失败',e.message,'error');}}
async function loadSchedules(){const data=await api('/admin/api/manage/schedules');scheduleRows.innerHTML=data.map(s=>`<tr><td>${s.id}</td><td>${s.doctor_name||s.doctorName}</td><td>${s.department_name||s.departmentName}</td><td>${s.schedule_date||s.scheduleDate}</td><td>${cnPeriod(s.period)}</td><td>${s.available_quota||s.availableQuota||0}</td><td class="table-actions"><button class="btn secondary" onclick="openScheduleForm(${s.id})">编辑</button><button class="btn danger" onclick="deleteSchedule(${s.id})">删除</button></td></tr>`).join('')||'<tr><td colspan="7">暂无排班</td></tr>';}
async function openScheduleForm(id){const data=await api('/admin/api/manage/schedules');const s=(data||[]).find(x=>x.id===id)||{};const dept=s.department_id||s.departmentId||departments[0]?.id;const {value:f}=await Swal.fire({title:id?'编辑排班':'给医生排班',html:`<input id="fDate" type="date" class="swal2-input" value="${s.schedule_date||s.scheduleDate||fmt(new Date())}"><select id="fDept" class="swal2-select" onchange="fDoctor.innerHTML=doctorOptions(this.value)">${deptOptions(dept)}</select><select id="fDoctor" class="swal2-select">${doctorOptions(dept,s.doctor_id||s.doctorId)}</select><select id="fPeriod" class="swal2-select"><option value="AM" ${s.period==='AM'?'selected':''}>上午</option><option value="PM" ${s.period==='PM'?'selected':''}>下午</option></select>`,focusConfirm:false,showCancelButton:true,confirmButtonText:'提交保存',cancelButtonText:'取消',preConfirm:()=>({scheduleDate:fDate.value,departmentId:fDept.value,doctorId:fDoctor.value,period:fPeriod.value})});if(!f)return;await api(id?`/admin/api/manage/schedules/${id}`:'/admin/api/manage/schedules',{method:id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});Swal.fire('保存成功','','success');loadSchedules();}
async function deleteSchedule(id){if(!(await Swal.fire({title:'确认删除排班？',text:'会同时删除该排班下所有号源时段',icon:'warning',showCancelButton:true,confirmButtonText:'删除',cancelButtonText:'取消'})).isConfirmed)return;await api(`/admin/api/manage/schedules/${id}`,{method:'DELETE'});loadSchedules();}
async function openSlotForm(){const data=await api('/admin/api/manage/schedules');const opts=(data||[]).map(s=>`<option value="${s.id}">${s.schedule_date||s.scheduleDate} ${cnPeriod(s.period)} ${s.doctor_name||s.doctorName}</option>`).join('');const {value:f}=await Swal.fire({title:'新增半小时号源时段',html:`<select id="fSchedule" class="swal2-select">${opts}</select><select id="fSlot" class="swal2-select">${halfHourSlots.map(x=>`<option value="${x}">${x}</option>`).join('')}</select><input id="fQuota" type="number" class="swal2-input" value="10" min="1" placeholder="号源数量">`,focusConfirm:false,showCancelButton:true,confirmButtonText:'新增号源',cancelButtonText:'取消',preConfirm:()=>({scheduleId:fSchedule.value,slot:fSlot.value,totalQuota:fQuota.value})});if(!f)return;const [startTime,endTime]=f.slot.split('-');await api('/admin/api/manage/slots',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({scheduleId:f.scheduleId,startTime,endTime,totalQuota:f.totalQuota})});Swal.fire('新增成功','','success');loadSchedules();}
initAdmin();
