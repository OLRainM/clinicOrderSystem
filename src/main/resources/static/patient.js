const today = new Date().toISOString().substring(0,10);document.getElementById('date').value=today;let schedules=[];
async function mockPatient(){const id=document.getElementById('userId').value||1001;await fetch(`/api/auth/mock-login?userId=${id}&username=patient${id}&role=PATIENT`,{method:'POST'});return id;}
async function loadDepartmentSchedules(){const dep=document.body.dataset.departmentId;const date=document.getElementById('date').value;const res=await fetch(`/api/schedules?departmentId=${dep}&date=${date}`);const json=await res.json();const grid=document.getElementById('doctorGrid');grid.innerHTML='';if(!json.success){grid.innerHTML=`<div class="card">${json.message}</div>`;return;}schedules=json.data;json.data.forEach(s=>{const total=s.slots.reduce((a,b)=>a+b.availableQuota,0);grid.insertAdjacentHTML('beforeend',`<div class="card"><h3>${s.doctorName}</h3><p class="muted">${s.departmentName} / ${s.period} / ${s.scheduleDate}</p><p>剩余号源：${total}</p><button class="btn" onclick="openSchedule(${s.id})">查看排班</button></div>`);});}
function openSchedule(scheduleId){const s=schedules.find(x=>x.id===scheduleId);document.getElementById('modalTitle').innerText=`${s.doctorName} ${s.scheduleDate} ${s.period}`;const box=document.getElementById('slotList');box.innerHTML='';s.slots.forEach(slot=>{box.insertAdjacentHTML('beforeend',`<div class="slot"><span>${slot.startTime}-${slot.endTime} ｜ 剩余 ${slot.availableQuota}</span><button class="btn success" onclick="reserve(${slot.id})">预约</button></div>`);});document.getElementById('scheduleModal').style.display='flex';}
function closeScheduleModal(){document.getElementById('scheduleModal').style.display='none';}
async function reserve(slotId){await mockPatient();const res=await fetch('/api/order/reserve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slotId})});const json=await res.json();if(json.success){await Swal.fire('抢号成功',`${json.data.message}<br/>订单号：${json.data.orderNo}`,'success');setTimeout(()=>location.href='/user/dashboard',800);}else{Swal.fire('预约失败',json.message,'error');}loadDepartmentSchedules();}
loadDepartmentSchedules();
